{
  "private": true,
  "type": "module",
  "scripts": {
    "check:renderer": "tsc -p RendererWeb/tsconfig.json --noEmit && tsc -p RendererWeb/tsconfig.vitest.json --noEmit",
    "build:renderer": "esbuild RendererWeb/src/renderer.ts --bundle --format=iife --target=es2020 --outfile=RendererWeb/assets/renderer.js --legal-comments=none",
    "gen:ipc-contract": "node RendererWeb/scripts/genIpcContract.mjs",
    "test:renderer": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "happy-dom": "^20.11.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
